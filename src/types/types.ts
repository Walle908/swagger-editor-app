export interface TextLocal {
  toolbar: {
    title: string;
    validStatus: string;
    invalidStatus: string;
    btnImport: string;
    btnSave: string;
  };
  editor: {
    title: string;
    lines: string;
  };
  viewer: {
    title: string;
    placeholder: string;
    filterPlaceholder: string;
  };
}
